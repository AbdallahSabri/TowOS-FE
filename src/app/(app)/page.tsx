// Placeholder. Phase 1 replaces this with the dispatch board (FE-SPEC.md §5).
export default function AppHomePage() {
  return (
    <main className="flex flex-1 items-center justify-center">
      <p>TowOS — Phase 0 bootstrap.</p>
    </main>
  );
}
