// Placeholder. Phase 0 slice 2 adds the real login form (react-hook-form + zod, FE-SPEC.md §10).
export default function LoginPage() {
  return (
    <main className="flex flex-1 items-center justify-center">
      <p>Login — coming in a later slice.</p>
    </main>
  );
}
